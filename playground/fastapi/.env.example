# Must match the secret key from the Novu dashboard environment you are testing against.
NOVU_SECRET_KEY=
NOVU_API_URL=https://api.novu.co
# Disable strict HMAC for unsigned local health-check probes.
NOVU_STRICT_AUTHENTICATION=false
