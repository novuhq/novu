# Optional: point at a local Novu stack (API + worker + WS must be running).
# Leave unset to use Novu cloud (default for quick try-it-out).
VITE_NOVU_BACKEND_URL=http://localhost:3000
VITE_NOVU_SOCKET_URL=http://localhost:3002
