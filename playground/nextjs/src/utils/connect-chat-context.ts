import type { ContextPayload } from '@novu/shared';

/** Shared playground context — keep in sync with NovuProvider and workflow triggers. */
export const connectChatContext: ContextPayload | undefined = { key: 'value2' };
// export const connectChatContext: ContextPayload | undefined = undefined;
