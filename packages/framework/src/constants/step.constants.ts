export enum ChannelStepEnum {
  EMAIL = 'email',
  SMS = 'sms',
  PUSH = 'push',
  CHAT = 'chat',
  IN_APP = 'in_app',
}

export enum ActionStepEnum {
  DIGEST = 'digest',
  DELAY = 'delay',
  CUSTOM = 'custom',
}
