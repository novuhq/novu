export enum ChannelStepEnum {
  EMAIL = 'email',
  SMS = 'sms',
  PUSH = 'push',
  CHAT = 'chat',
  IN_APP = 'in_app',
}

export enum ActionStepEnum {
  DIGEST = 'digest',
  DELAY = 'delay',
  THROTTLE = 'throttle',
  CUSTOM = 'custom',
  HTTP_REQUEST = 'http_request',
}
