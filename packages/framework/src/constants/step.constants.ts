export enum ChannelStepEnum {
  EMAIL = 'email',
  SMS = 'sms',
  PUSH = 'push',
  CHAT = 'chat',
  IN_APP = 'in_app',
  TOOL = 'tool',
}

export enum ActionStepEnum {
  DIGEST = 'digest',
  DELAY = 'delay',
  WAIT = 'wait',
  THROTTLE = 'throttle',
  CUSTOM = 'custom',
  HTTP_REQUEST = 'http_request',
}
