export enum PostActionEnum {
  TRIGGER = 'trigger',
  EXECUTE = 'execute',
  PREVIEW = 'preview',
}

export enum GetActionEnum {
  DISCOVER = 'discover',
  HEALTH_CHECK = 'health-check',
  CODE = 'code',
}
