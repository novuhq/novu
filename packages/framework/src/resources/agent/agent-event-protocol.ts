/**
 * AgentEvent wire protocol — canonical source is @novu/agent-event-protocol
 * (private workspace package, inlined into this bundle at build time).
 */
export * from '@novu/agent-event-protocol';
