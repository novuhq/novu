{
  "name": "@novu/chat-adapter-web",
  "sourceRoot": "packages/chat-adapter-web/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @novu/chat-adapter-web run build",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx biome lint packages/chat-adapter-web"
      }
    }
  },
  "tags": ["type:package"]
}
