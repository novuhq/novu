import { expect } from '@jest/globals';

describe('test use of novus node package - Events', () => {
  test('should ss', () => {
    expect(1).toBe(1);
  });
  test('should fail', () => {
    expect(1).toBe(2);
  });
});
