export const MCP_OAUTH_CALLBACK_PATH = '/v1/agents/mcp/oauth/callback';

/** OAuth state lifetime — must complete the round-trip within this window. */
export const MCP_OAUTH_STATE_TTL_MS = 5 * 60 * 1000;
