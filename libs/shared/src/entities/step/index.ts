export enum DigestUnitEnum {
  SECONDS = 'seconds',
  MINUTES = 'minutes',
  HOURS = 'hours',
  DAYS = 'days',
}

export enum DigestTypeEnum {
  REGULAR = 'regular',
  BACKOFF = 'backoff',
}

export enum DelayTypeEnum {
  REGULAR = 'regular',
  SCHEDULED = 'scheduled',
}
