export enum ChannelTypeEnum {
  IN_APP = 'in_app',
  EMAIL = 'email',
  SMS = 'sms',
  DIRECT = 'direct',
  PUSH = 'push',
  DIGEST = 'digest',
  TRIGGER = 'trigger',
}

export enum ChannelCTATypeEnum {
  REDIRECT = 'redirect',
}
