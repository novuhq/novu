/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AgentPlanUsageDto,
  AgentPlanUsageDto$inboundSchema,
} from "./agentplanusagedto.js";
import {
  AgentResponseDto,
  AgentResponseDto$inboundSchema,
} from "./agentresponsedto.js";

export type ListAgentsResponseDto = {
  /**
   * List of returned agents
   */
  data: Array<AgentResponseDto>;
  /**
   * The cursor for the next page of results, or null if there are no more pages.
   */
  next: string | null;
  /**
   * The cursor for the previous page of results, or null if this is the first page.
   */
  previous: string | null;
  /**
   * The total count of items (up to 50,000)
   */
  totalCount: number;
  /**
   * Whether there are more than 50,000 results available
   */
  totalCountCapped: boolean;
  /**
   * Cloud only. Active agent usage in this environment against the organization plan limit. Agents created beyond the limit are flagged with `exceedsPlanLimit` and will not respond to inbound messages.
   */
  planUsage?: AgentPlanUsageDto | undefined;
};

/** @internal */
export const ListAgentsResponseDto$inboundSchema: z.ZodType<
  ListAgentsResponseDto,
  z.ZodTypeDef,
  unknown
> = z.object({
  data: z.array(AgentResponseDto$inboundSchema),
  next: z.nullable(z.string()),
  previous: z.nullable(z.string()),
  totalCount: z.number(),
  totalCountCapped: z.boolean(),
  planUsage: AgentPlanUsageDto$inboundSchema.optional(),
});

export function listAgentsResponseDtoFromJSON(
  jsonString: string,
): SafeParseResult<ListAgentsResponseDto, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListAgentsResponseDto$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListAgentsResponseDto' from JSON`,
  );
}
