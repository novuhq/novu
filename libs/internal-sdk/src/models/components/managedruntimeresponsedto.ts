/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  AgentMcpServerDto,
  AgentMcpServerDto$inboundSchema,
} from "./agentmcpserverdto.js";
import { AgentToolDto, AgentToolDto$inboundSchema } from "./agenttooldto.js";

export type ManagedRuntimeResponseDto = {
  providerId: string;
  integrationId: string;
  externalAgentId: string;
  /**
   * The provider-side environment that hosts this agent. Hydrated from the linked integration credentials. Absent when the integration has not been provisioned.
   */
  externalEnvironmentId?: string | undefined;
  /**
   * The provider-side workspace id used in console deep links. Defaults to `'default'` (the auto-created Default Workspace). Hydrated from the linked integration credentials.
   */
  externalWorkspaceId?: string | undefined;
  /**
   * Deep link to the agent in the provider console (e.g. platform.claude.com).
   */
  consoleUrl?: string | undefined;
  /**
   * Live tools enabled on the managed-runtime provider for this agent. Sourced from the provider on read; omitted when the provider is temporarily unreachable.
   */
  tools?: Array<AgentToolDto> | undefined;
  /**
   * MCP servers enabled on this agent, projected onto the runtime catalog `{ externalId, name, url }` shape. Mongo is the source of truth; the projection matches what the provider sees. Omitted when the provider read fails after a managed-agent write.
   */
  mcpServers?: Array<AgentMcpServerDto> | undefined;
  /**
   * System prompt used when invoking the agent.
   */
  systemPrompt?: string | undefined;
};

/** @internal */
export const ManagedRuntimeResponseDto$inboundSchema: z.ZodType<
  ManagedRuntimeResponseDto,
  z.ZodTypeDef,
  unknown
> = z.object({
  providerId: z.string(),
  integrationId: z.string(),
  externalAgentId: z.string(),
  externalEnvironmentId: z.string().optional(),
  externalWorkspaceId: z.string().optional(),
  consoleUrl: z.string().optional(),
  tools: z.array(AgentToolDto$inboundSchema).optional(),
  mcpServers: z.array(AgentMcpServerDto$inboundSchema).optional(),
  systemPrompt: z.string().optional(),
});

export function managedRuntimeResponseDtoFromJSON(
  jsonString: string,
): SafeParseResult<ManagedRuntimeResponseDto, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ManagedRuntimeResponseDto$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ManagedRuntimeResponseDto' from JSON`,
  );
}
