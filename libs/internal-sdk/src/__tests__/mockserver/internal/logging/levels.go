// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package logging

import (
	"fmt"
	"log/slog"
	"strings"
)

const (
	// Debug log level.
	LevelDebug = "DEBUG"

	// Info log level.
	LevelInfo = "INFO"

	// Warning log level.
	LevelWarn = "WARN"

	// Error log level.
	LevelError = "ERROR"

	// Default log level.
	DefaultLevel = LevelInfo
)

// Levels returns all supported levels.
func Levels() []string {
	return []string{
		LevelDebug,
		LevelInfo,
		LevelWarn,
		LevelError,
	}
}

func levelFromString(level string) (*slog.LevelVar, error) {
	result := new(slog.LevelVar)

	switch strings.ToUpper(level) {
	case "DEBUG":
		result.Set(slog.LevelDebug)
	case "INFO":
		result.Set(slog.LevelInfo)
	case "WARN":
		result.Set(slog.LevelWarn)
	case "ERROR":
		result.Set(slog.LevelError)
	default:
		return nil, fmt.Errorf("unsupported log level (%s), supported levels: %s", level, strings.Join(Levels(), ", "))
	}

	return result, nil
}
