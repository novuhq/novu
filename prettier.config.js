/** @type {import("prettier").Config} */
const config = {
  printWidth: 120,
  trailingComma: 'es5',
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  quoteProps: 'as-needed',
  jsxSingleQuote: false,
  arrowParens: 'always',
};

export default config;
